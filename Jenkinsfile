pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'npm install'
                sh 'npm install bcrypt'
            }
        }
        stage('Test') { 
            steps {
                echo "Test"
            }
        }
        stage('Deploy') { 
            steps {
                echo "Test"
            }
        }
    }
}